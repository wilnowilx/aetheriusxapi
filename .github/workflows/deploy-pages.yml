name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      # Detect repo name to set correct base path
      - name: Set build base
        id: base
        run: |
          REPO="${{ github.repository }}"
          if echo "$REPO" | grep -q "staging"; then
            echo "pbase=/aetheriusxapi-staging/" >> $GITHUB_OUTPUT
          else
            echo "pbase=/aetheriusxapi/" >> $GITHUB_OUTPUT
          fi

      - name: Build
        working-directory: frontend
        run: node scripts/build-staging.mjs
        env:
          PAGES_BASE: ${{ steps.base.outputs.pbase }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
